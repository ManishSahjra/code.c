#include<stdio.h>
int main(){
    int x,sum;
    printf("Enter the number of times you want to print : ");
    scanf("%d",&x);
    sum=0;
    for (int i=1;i<=x;i++){
        sum=sum+i;
        
    }printf("%d",sum);

    return 0;
}
